# Application's direct dependencies, pinned tightly.
# Install with 'pip install -r requirements/default.in
docopt==0.6.1

# Plus this source, in develop/editable mode.
# (This also installs anything under setup.py's 'install_requires')
-e .

